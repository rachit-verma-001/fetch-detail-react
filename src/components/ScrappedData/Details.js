import { Fragment } from 'react';
import AvailableData from './AvailableData';

const Details = () => {
  return (
    <Fragment>
      <AvailableData />
    </Fragment>
  );
};

export default Details;
